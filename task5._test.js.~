  var = getElementById(mySquare, colorName);
    function.setBackgroundColor(id, colorName)

  if(colorName){
    id.style.backgroundColor = colorName;
      window.alert("fails");
  }
  else {
      window.alert("fails");
  }
}

